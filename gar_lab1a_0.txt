-- работа студента
-- допустим он сделал ошибки

create table employees ( -- таблицу неправильно назвал
	id int not null,
	name TEXT not null,
	birthday DATETIME NULL,
	sex TEXT not NULL, 
	active BOOL default FALSE, -- непрвильно указал тип (BOOL)
	salary int not NULL
);
